# Flash-card brief validation notes

On the live `/library` route, the Content Missions launch/readiness brief now renders as a four-card interactive deck. The visible copy confirms role-chip filtering, a flash-card container with the hint `Tap, click, or press Enter to flip`, and deck controls labeled `Previous card`, `Flip card`, and `Next card`. The extracted page text also confirms that the launch brief now uses the learner-lane wording and keeps the launch-readiness content inside a card sequence rather than static tiles.

On the live `/training` route, the Training Zone lesson brief now renders inside the course canvas as an interactive flash-card surface. The extracted page text confirms the new explanatory sentence about the lesson brief reading like a guided flash-card deck, and the viewport shows the active flash-card with the same navigation controls. This validates that both requested surfaces now share the flash-card interaction pattern in the running demo.
