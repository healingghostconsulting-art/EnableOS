# Google Doc implementation prompts — resumed run on 2026-05-14

This file refreshes the shared Google Doc audit for the resumed EnableOS run and identifies the current safe implementation scope.

| Source line | Implementation-ready prompt |
| --- | --- |
| How to be the human in the loop to work with agent assist tools | **Keep the learner-facing human-in-the-loop guidance in place and verify it remains visible and correct in the learner training route.** This item is already implemented in the project but is still not marked in the source document. Do not change the product further unless a regression is found. Validation expectation: confirm the guidance remains present in the learner flow and update the source doc only when reliable editing access is available. |
| To many words for the content | **Keep the reduced-density learner training copy in place and verify that the top learner training shell remains concise and readable.** This item is already implemented in the project but is still not marked in the source document. Validation expectation: confirm the trimmed copy remains visible and coherent in the learner flow before updating the source doc. |
| To dark-mark it happy | **Keep the lighter learner training presentation refinements in place and verify they still preserve the existing palette direction.** This item is already implemented in the project but is still not marked in the source document. Validation expectation: confirm the brighter learner-facing panels still render correctly before updating the source doc. |
| Branding it to be it’s own business EnableOS powered by CHC | **Refine the most visible brand hierarchy so the experience reads as EnableOS first, while preserving CHCG as the supporting organization through a secondary ‘powered by CHCG’ treatment.** Focus on the public-facing hero or other top-level identity surfaces that currently foreground CHCG too heavily. Keep trust, enterprise tone, and the current layout direction intact. Validation expectation: confirm the revised hierarchy is visible on the main public-facing surface and remains visually consistent with the rest of the site. |
| Questions Reporting | **Add assessment-question reporting inside reporting surfaces so managers can see frequently missed questions and weak knowledge areas.** This requires identifying an existing reporting flow, defining data sources, and validating any new logic through targeted tests plus UI review. |
| Agent lifecycle with tenure | **Add tenure-aware reporting context to analytics so results can be interpreted by employee lifecycle stage.** Reuse seeded data when possible and validate any derived transformations with deterministic tests. |
| Give % based on peers | **Add peer-relative percentage or percentile context to reporting surfaces so comparisons are explainable and clearly labeled.** Validate comparison calculations with targeted tests and verify the display in the UI. |
| High-alert for questions that are low | **Surface high-alert indicators for low-performing assessment questions in reporting views.** Reuse existing question and reporting structures, and validate threshold logic with tests plus UI review. |
| How do you prove that it’s working | **Add a scoped proof-of-impact treatment to reporting that shows how training and coaching are affecting outcomes over time.** Prefer a focused evidence card or explanatory panel over a broad redesign. |
| This is the 3 time you’ve sent the module | **Show repeated reassignment count and escalation context for the same module in management views.** Reuse assignment history if available and validate derived counts with targeted tests. |
| Measureing coaching consistent | **Add coaching-consistency visibility to management reporting through a focused metric or card.** Keep scope modest and validate any derived data calculations with tests. |
| Behavior analysis | **Add a lightweight behavior-analysis reporting layer grounded in existing seeded data and flows.** Keep scope narrow enough to remain demonstrable without introducing a major subsystem. |
| Dashboard | **Only expand dashboard surfaces through specific additive panels with a defined role, location, data source, and validation path.** Leave this unimplemented until a narrower slice is selected. |
| Error rate | **Expose error-rate reporting within ROI or operational reporting surfaces so training and coaching can be tied to error reduction.** Validate any transformation logic through deterministic tests and confirm the UI display. |
| Agent Profile — Name / Avatar / Start date / Ranking score vs time completed / Module History / When did I trainged / Skill | **Build or refine a scoped agent-profile summary view that surfaces identity, tenure, skill focus, and completion history without introducing a full new subsystem.** Validate role scoping and any derived profile metrics with targeted tests and UI review. |
| The coaching log screen can be separate screen or pop up box | **Refine the coaching-log entry experience so it can appear cleanly as a dedicated screen or dialog while preserving current permissions and data behavior.** Validate by opening the coaching-log flow and confirming usability. |
| Give the ability to import their corprate training | **Add a tenant-safe corporate-training import flow that protects client isolation and preserves CHCG core material visibility.** This likely requires upload-flow design and storage considerations before implementation. |
| Make a Develoopment & Launch Roadmap | **Produce a development and launch roadmap artifact that translates the current product scope into phased milestones, dependencies, validation gates, and ownership lanes.** This is best handled as a document deliverable rather than an immediate in-app code change. |
| LEAN Methodolog - Wayne | **Hold this item as blocked because it is too ambiguous to implement safely without clarifying whether it refers to content, taxonomy, framework positioning, or workflow behavior.** |
| How to demonstrate more gamification and engagement | **Add clearer gamification and engagement signals to the learner or manager flow through a modest, product-appropriate enhancement such as stronger progress recognition, achievement framing, or motivation cues.** Validate in the targeted role flow and avoid gimmicky mechanics that conflict with the enterprise tone. |
| How | **Hold this item as blocked because the source line is incomplete and does not provide enough information to implement safely.** |

## Selected safe implementation scope for the resumed run

The next highest-priority safe implementation scope is **brand hierarchy refinement on the most visible public-facing EnableOS surface**, because it is specific, visually inspectable, and meaningfully advances the open line about positioning the product as **EnableOS powered by CHCG** without requiring new backend data structures.

## Refreshed continuation decision

A fresh code audit shows that the product already has a substantial weekly coaching-log surface across executive, coach, manager, learner, and client-admin flows, including capture forms, timeline views, and at least one dialog-backed presentation path inside `client/src/pages/EnableOSViews.tsx`. The next highest-priority safe item is therefore to **tighten the coaching-log experience so it is clearly available as a separate focused overlay from the manager workflow**, matching the source note that the coaching log can be a separate screen or pop-up box.

### Implementation-ready prompt

**Add a clear manager-facing coaching-log launch path that opens the weekly coaching log in a focused dialog while preserving the existing inline coaching workflow and data behavior.** Scope the change to the manager flow so the request is satisfied through a visible, testable entry point without broad restructuring. Reuse the current structured coaching-log components and keep permissions, save behavior, and existing tabs intact. Validation expectation: add targeted regression coverage for the new dialog-launch affordance, run the relevant Vitest coverage, and confirm in the live manager workspace that the dialog entry point is visible and usable.
