# Google Doc Implementation Prompts — 2026-05-15

This run re-audited the shared Google Doc and treated every actionable line without a `[Done]` prefix as open work. The already completed learner-readability, brand-hierarchy, coaching-log pop-up, and question-reporting items remain open in the source document only because the live Google Docs editor still requires manual inline confirmation for reliable edits.

| Source line | Implementation-ready prompt | Status for this run |
| --- | --- | --- |
| `How to be the human in the loop to work with agent assist tools` | Add concise learner-facing and manager-facing guidance that frames agent-assist features as decision support rather than replacement, explains where human review is required, and preserves the existing workflow structure. Validate with targeted layout tests and live UI review before marking complete. | Already implemented and validated earlier; blocked only on manual `[Done]` edit in the source doc. |
| `To many words for the content` | Reduce dense learner-facing training copy in the top training shell and related context panels, keeping the same information architecture while improving scanability and preserving the established visual direction. Validate with targeted layout tests and live learner-route review before marking complete. | Already implemented and validated earlier; blocked only on manual `[Done]` edit in the source doc. |
| `To dark-mark it happy` | Brighten the learner training top-shell presentation and supporting panels without abandoning the current palette, increasing clarity and warmth while keeping the premium visual system intact. Validate with targeted layout tests and live learner-route review before marking complete. | Already implemented and validated earlier; blocked only on manual `[Done]` edit in the source doc. |
| `Branding it to be it’s own business EnableOS powered by CHC` | Refine the public-facing landing hero so EnableOS leads the visible product hierarchy and CHCG appears as a secondary powered-by layer, without weakening CHCG methodology credibility. Validate with targeted layout tests and live landing-page review before marking complete. | Already implemented and validated earlier; blocked only on manual `[Done]` edit in the source doc. |
| `Questions Reporting` plus related lines `Give % based on peers` and `High-alert for questions that are low` | Add an executive reporting panel that surfaces the most-missed assessment questions, miss rate, peer baseline, peer percentile, and a clear high-alert label for the lowest-performing items. Seed stable demo data, cover the response and UI with targeted regression tests, and verify the executive route in the live preview before marking complete. | Implemented and validated in this run; blocked only on manual `[Done]` edit in the source doc. |
| `Agent lifecycle with tenure` | Extend the executive reporting surface with a tenure-aware lifecycle view that shows how performance, readiness, or intervention outcomes change across early, developing, and tenured employee stages. Reuse the seeded demo data model where possible, keep the panel focused and presentation-ready, and validate with targeted tests plus live executive-route review. | Selected as the next highest-priority safe implementation candidate for this run. |
| `How do you prove that it’s working` | Add an executive proof-of-impact panel that explains outcome evidence using before/after movement, intervention correlation, and sustained-readiness indicators. Keep claims demonstrable from the seeded data and avoid unsupported causal language. Validate with targeted tests and live executive-route review. | Open but broader than the lifecycle item; defer until the next safe pass. |
| `Measureing coaching consistent` | Add a coaching-consistency reporting surface that shows cadence adherence, missed coaching intervals, or completion consistency across managers. Reuse existing coaching-log data where possible and validate with targeted tests and live review. | Open but requires broader scoping than the lifecycle item. |
| `Behavior analysis` | Add a focused behavior-analysis summary for leaders that groups coaching observations into a small number of explainable behavior themes tied to actions and outcomes. Keep it demonstrative, not diagnostic, and validate with targeted tests and UI review. | Open but broader and more interpretive than the lifecycle item. |
| `Error rate` | Add a compact error-rate reporting metric or trend to the executive analytics area using existing seeded KPI structures where possible. Ensure the metric is clearly defined and visually consistent with the current ROI surface. Validate with targeted tests and live review. | Open, but lower priority than lifecycle reporting. |
| `Agent Profile` details | Extend the agent profile view with presentation-ready identity, tenure, module history, and ranking context. Keep scope limited to seeded demo data and role-appropriate visibility. Validate with targeted tests and live review. | Open, but larger than the lifecycle reporting enhancement. |
| `Give the ability to import their corprate training` | Strengthen the client-content import story so customers can see how corporate training enters the platform, how it is labeled, and where it becomes available. Reuse the existing upload and library flow wherever possible. Validate with targeted tests and live review. | Potentially already partly addressed; requires duplicate-gap audit before implementation. |
| `Make a Develoopment & Launch Roadmap` | Create a clear internal roadmap artifact or in-product summary that explains phased implementation and launch readiness for the demo. Keep it presentation-ready and aligned with current platform capabilities. Validate by reviewing the artifact or UI surface in context. | Open, but not the highest-priority product interaction item. |
| `How to demonstrate more gamification and engagement` | Add or refine lightweight engagement cues such as progress framing, achievement treatment, or motivation copy without turning the enterprise product into a consumer-style game. Validate with targeted layout tests and live learner review. | Open, but broader than the lifecycle reporting item. |

The next safest implementation candidate is **`Agent lifecycle with tenure`** because it fits the existing executive reporting surface, reuses seeded demo-data patterns, aligns with the already-added question-reporting enhancement, and can be validated with focused server and layout tests plus a live executive-route review.

## Refreshed next-scope decision after lifecycle reporting

The next highest-priority safe candidate is **`How do you prove that it’s working`**. A focused implementation prompt is: add an executive proof-of-impact panel that summarizes before/after movement, intervention correlation, and sustained-readiness evidence using seeded demo metrics already present in the executive analytics surface. Keep the language evidence-based rather than causal, avoid introducing unsupported claims, and validate with targeted server and layout tests plus a live executive-route review before any source-document update.
