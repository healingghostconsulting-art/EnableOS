# Google Doc Implementation Prompts — 2026-05-14

This file translates each newly open actionable line from the shared Google Doc into an implementation-ready prompt for the CHCG EnableOS Demo at `/home/ubuntu/chcg-enableos-demo`.

| Source line | Implementation-ready prompt |
| --- | --- |
| How to be the human in the loop to work with agent assist tools | **Implement a learner-facing human-in-the-loop guidance treatment inside the training experience** so EnableOS clearly frames agent-assist tools as support rather than replacement. Apply the change in the learner training flow, especially near the top course-player context where users orient themselves before beginning a module. Keep the guidance concise, practical, and compatible with the existing CHCG/EnableOS voice. Validate by confirming the new copy is visible in the learner training route and does not break existing training progression or layout behavior. |
| To many words for the content | **Reduce content density in the learner training shell without removing required learning structure**. Prioritize the top training preview and active-module context panels where long explanatory paragraphs currently make the experience feel heavy. Rewrite copy into shorter, clearer learner-facing language while preserving the meaning of the training flow, module context, and validation cues. Validate by reviewing the learner training route and ensuring the updated text remains readable, consistent, and complete across the affected surfaces. |
| To dark-mark it happy | **Brighten the learner training presentation while preserving the established color palette direction**. Target dark, heavy learner-facing training cards and preview panels that can be lightened or softened without changing the overall brand palette. Improve perceived energy and readability through safer background, border, and emphasis adjustments rather than a full redesign. Validate by confirming contrast remains strong and the updated surfaces still feel visually consistent with the rest of EnableOS. |
| Branding it to be it’s own business EnableOS powered by CHC | **Refine the product-brand framing so the experience reads as EnableOS first, with CHCG presented as the powering or parent organization**. Apply the change to the most visible brand surfaces only if the current shell still over-emphasizes CHCG at the expense of the product identity. Preserve trust, enterprise tone, and current tenant-aware behavior. Validate by reviewing the affected header or hero surfaces and confirming the updated hierarchy reads clearly. |
| Questions Reporting | **Design and implement assessment-question reporting for EnableOS** so managers or leaders can inspect which questions are frequently missed, where comprehension breaks down, and which modules need reinforcement. Scope the change to existing reporting surfaces and seeded data patterns. Validate with targeted tests for any new transformation logic and with UI review for the surfaced metrics. |
| Agent lifecycle with tenure | **Add tenure-aware reporting to the EnableOS analytics experience** so outcomes can be interpreted against employee tenure bands or lifecycle stages. Reuse existing seeded learner and performance structures where possible. Validate by confirming the new reporting reads coherently and does not break current dashboards. |
| Give % based on peers | **Add peer-relative percentage or percentile context to existing reporting surfaces** so managers can compare a learner or team against comparable peers. Keep the comparison logic explainable and clearly labeled. Validate through deterministic tests for the comparison calculation and browser review for the display. |
| High-alert for questions that are low | **Surface high-alert flags for low-performing assessment questions** in reporting or oversight views so weak question performance is visible without manual scanning. Reuse existing assessment and reporting data structures where possible. Validate with tests around threshold logic and a UI check for alert visibility. |
| How do you prove that it’s working | **Add clearer proof-of-impact framing to EnableOS reporting** so stakeholders can see how training, coaching, and retraining are affecting outcomes over time. Prefer scoped ROI evidence cards or supporting explanations over a broad redesign. Validate by reviewing the updated reporting narrative and any supporting metrics. |
| This is the 3 time you’ve sent the module | **Show repeated retraining assignment count and escalation context** so managers and coaches can see when a learner has been assigned the same module multiple times. Reuse assignment history if available. Validate with logic tests and browser review in the oversight flow. |
| Measureing coaching consistent | **Add coaching-consistency visibility to management reporting** so leaders can inspect whether coaching cadence and follow-through are happening consistently across the team. Prefer a scoped metric or card over a large dashboard rewrite. Validate with data tests and UI review. |
| Behavior analysis | **Introduce a behavior-analysis reporting layer** that ties training, coaching, and performance signals into observable behavior trends. Keep scope modest and grounded in the existing demo model. Validate by confirming the narrative and metrics align with current seeded data. |
| Dashboard | **Expand the reporting dashboard only through clearly defined, additive panels** rather than a broad redesign. Any implementation should identify the role, panel location, data source, and validation path before coding begins. |
| Error rate | **Expose error-rate reporting inside the ROI or operational reporting surfaces** so users can connect training and coaching activity to error reduction. Reuse existing seeded signal patterns if available. Validate with deterministic data output and UI review. |
| Agent Profile — Name / Avatar / Start date / Ranking score vs time completed / Module History / When did I trainged / Skill | **Build or refine an agent-profile view for the learner or oversight flow** that includes identity, start date, skill focus, completion history, and ranking-oriented context without breaking current role scoping. Scope the first pass to a presentation-ready profile summary rather than a fully new subsystem. Validate through role-aware UI review and targeted tests for any derived profile metrics. |
| The coaching log screen can be separate screen or pop up box | **Confirm or refine the coaching-log entry experience so it can be accessed cleanly as its own focused surface or dialog**. Preserve current role permissions and weekly-coaching data behavior. Validate by opening the coaching log flow and confirming it remains usable. |
| Give the ability to import their corprate training | **Support tenant-safe import of corporate training materials into EnableOS** so client content can be blended into journeys and library workflows without diluting CHCG core material visibility. Validate through upload-flow and tenant-isolation checks. |
| Make a Develoopment & Launch Roadmap | **Produce a development and launch roadmap artifact for EnableOS** that translates current product scope into phased delivery milestones, launch dependencies, validation gates, and ownership lanes. This is document work rather than an in-app code change. |
| LEAN Methodolog - Wayne | **Determine whether LEAN methodology should be added as content, taxonomy, or workflow framing** before implementation. This line is currently too ambiguous to complete safely without clarification. |
| How to demonstrate more gamification and engagement | **Add clearer gamification and engagement signals to the learner or manager experience** through scoped UI enhancements such as achievement framing, visible streak/progress cues, or recognition-oriented prompts that fit the existing platform. Validate by confirming the change is visible in the intended role flow and remains consistent with the product tone. |
| How | **No implementation prompt yet** because the source line is incomplete. Leave unmarked until the request is clarified in the source document. |

## Selected safe implementation scope for this run

The highest-priority safe work for this pass is a combined learner training refinement that addresses three related open lines together: **human-in-the-loop guidance**, **reduced content density**, and **a lighter, happier training presentation without changing the core palette**. This scope is specific enough to complete safely within the existing learner training route and can be validated with targeted tests plus project health checks.
